{
  "name": "purescript-starter-template",
  "author": "Risto Stevcev",
  "version": "1.0.0",
  "description": "A purescript starter template using purescript-signal and purescript-smolder-vdom",
  "license": "MIT",
  "main": "entry.js",
  "directories": {
    "src": "src",
    "test": "test"
  },
  "scripts": {
    "pulp:watch": "pulp --watch build -- --censor-lib --stash",
    "pulp:build": "pulp build -- --censor-lib --stash",
    "pulp:run": "npm run pulp:build && pulp run",
    "pulp:test": "pulp build -I test -- --censor-lib --stash && pulp test",
    "build": "npm run pulp:build && ./node_modules/.bin/webpack",
    "clean": "rm -rf .pulp-cache output bundle.js"
  },
  "devDependencies": {
    "http-server": "^0.9.0",
    "purescript-psa": "^0.4.0",
    "webpack": "^2.2.1"
  }
}
